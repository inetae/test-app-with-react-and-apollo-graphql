export interface Testimonial {
    id?: string;
    firstName?: string;
    lastName?: string;
    message?: string;
}

export interface Post {
    id: string;
    title: string;
    content: string;
}
